name: Dependabot Check

on:
  pull_request:
    types:
      - synchronize
      - opened

jobs:
  dependabot-check:
    name: Dependabot Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Dependabot Checks
        run: |
          # Retrieve vulnerability report from Dependabot API
          report=$(curl -s -H "Authorization: Bearer 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" \
            "https://api.github.com/repos/ad-infra/dependabot/vulnerabilities")

          # Check if Severity: High vulnerability is present in the report
          if echo "$report" | jq '.[] | select(.severity == "High")' > /dev/null; then
            echo "Severity: High vulnerability found. Blocking the pull request."
            exit 1
          else
            echo "No Severity: High vulnerabilities found. Pull request can proceed."
            exit 0
          fi
